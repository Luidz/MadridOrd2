<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ldz.madridord2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal" android:label="Chuletas PMM">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActividadMovilidad" android:label="Ordenanza Movilidad Sostenible" />
        <activity android:name=".ActividadMovilidadLista" android:label="Ordenanza Movilidad Sostenible"/>
        <activity android:name=".ActividadOrdenanzas" android:label="Chuletario"/>
        <activity android:name=".ActividadOrdenanzasLista" android:label="Chuletas PMM"/>
    </application>

</manifest>